import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

export default function DestinationDetails({route} : any) {
  // const id = route.props.id
  return (
    <View>
      <Text>ID = </Text>
    </View>
  )
}

const styles = StyleSheet.create({})